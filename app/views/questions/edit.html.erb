<h1>Editing Question</h1>

<%= render 'form', question: @question %>

<%= link_to 'Show', organization_quiz_question_path(@organization, @quiz, @question) %> |
<%= link_to 'Back', organization_quiz_questions_path(@organization, @quiz) %>
